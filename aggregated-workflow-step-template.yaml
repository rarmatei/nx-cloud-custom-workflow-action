name: "NxCloud Aggregated Example"
description: "Example of an aggregated workflow step template"

definition:
  using: 'aggregated'
  steps:
    - name: 'Aggregated step 1'
      script: "echo 'Echo step 1 aggregated'"
    - name: 'Example of aggregated template using another workflow step'
      uses: "rarmatei/nx-cloud-custom-workflow-action/main/node-workflow-step-template.yaml"
    - name: 'Aggregated step 3'
      script: "echo \"example of aggregated step with logging an arg >>$ARG_2<<\""